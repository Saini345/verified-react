import React from 'react'

function Individual_ZeroBalance(props) {
    const individual_ZeroBalance = props.Individual_ZeroBalance ? props.Individual_Pages: "";
  return (
    <div>{props.individual_ZeroBalance}</div>
  )
}

export default Individual_ZeroBalance;
